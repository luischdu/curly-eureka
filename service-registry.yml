server:
  port: ${PORT:8761}

eureka:
  server:
    enableSelfPreservation: false
  client:
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://service-registry:${server.port}/eureka
  instance:
    preferIpAddress: true